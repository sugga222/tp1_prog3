// Config centralizada de la API
// Cambiá `API_URL` según el entorno (localhost en desarrollo, URL de Render en producción)
const CONFIG = {
    API_URL: "http://localhost:3000"
};

// Nota: los scripts usan `API_BASE` como fallback cuando `CONFIG` no está cargado.
